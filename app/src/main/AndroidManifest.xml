<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kakiridev.simpleenglishwords">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartView" />
        <activity android:name=".WordListView" />
        <activity
            android:name=".AddWordView"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".FB_Login.LoginView"></activity>
        <activity android:name=".FB_Login.LoginProfile" />
        <activity android:name=".FB_Login.Login" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Testowe"></activity>
    </application>

</manifest>