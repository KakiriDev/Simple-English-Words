<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kakiridev.simpleenglishwords">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8343407965657663~4839584987"/>

        <activity android:name=".MainView"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".ShowListWords.WordListView" />

        <activity android:name=".KnowWords.RandW"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".LoginView.LoginView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DataLoading"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".RoundView"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".Statistic"
            android:theme="@style/AppTheme.NoActionBar"/>

    </application>

</manifest>